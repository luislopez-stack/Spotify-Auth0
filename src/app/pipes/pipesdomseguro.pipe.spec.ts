import { PipesdomseguroPipe } from './pipesdomseguro.pipe';

describe('PipesdomseguroPipe', () => {
  it('create an instance', () => {
    const pipe = new PipesdomseguroPipe();
    expect(pipe).toBeTruthy();
  });
});
